<Window x:Class="FinanzasPersonales.NET.Views.EstadisticaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Finanzas Personales - Estadísticas"
        Height="820"
        Width="760"
        MinHeight="640"
        MinWidth="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">

            <TextBlock Text="ESTADÍSTICAS FINANCIERAS"
                       FontSize="24"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,8" />

            <TextBlock Text="Resumen de tu situación financiera"
                       FontSize="14"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,16" />

            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="Ver Resumen General"
                        Height="36"
                        Click="BtnResumenGeneral_Click" />
                <Button Grid.Column="2"
                        Content="Ver Gastos por Categoría"
                        Height="36"
                        Click="BtnGastosPorCategoria_Click" />
                <Button Grid.Column="4"
                        Content="Ver Resumen Mensual"
                        Height="36"
                        Click="BtnResumenMensual_Click" />
                <Button Grid.Column="6"
                        Content="Ver Todo"
                        Height="36"
                        Click="BtnVerTodo_Click" />
            </Grid>

            <Button Content="Ver Gráficas Visuales"
                    Height="36"
                    Margin="0,0,0,14"
                    Background="#2088ff"
                    Foreground="White"
                    FontWeight="SemiBold"
                    Click="BtnVerGraficas_Click" />

            <TextBlock Text="Enviar resumen a:"
                       FontSize="14"
                       Margin="0,0,0,5" />
            <TextBox x:Name="txtCorreoDestino"
                     Height="30"
                     Margin="0,0,0,10"
                     VerticalContentAlignment="Center"
                     Padding="5,0" />

            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="Enviar por Correo Electrónico"
                        Height="36"
                        Click="BtnEnviarCorreo_Click" />

                <Button Grid.Column="2"
                        Content="Volver al Menú Principal"
                        Height="36"
                        Padding="14,0"
                        Click="BtnVolver_Click" />
            </Grid>

            <TextBlock x:Name="txtMensaje"
                       Text=""
                       TextWrapping="Wrap"
                       Margin="0,0,0,8" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,8,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="txtTablaTitulo"
                       FontWeight="Bold"
                       FontSize="13"
                       Margin="0,0,0,6" />

            <DataGrid x:Name="dgDatos"
                      Grid.Row="1"
                      AutoGenerateColumns="True"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      GridLinesVisibility="All"
                      AlternatingRowBackground="#F4F7FB"
                      RowBackground="White"
                      BorderBrush="#D0D7DE"
                      BorderThickness="1"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto" />
        </Grid>
    </Grid>
</Window>
