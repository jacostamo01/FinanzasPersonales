<Window x:Class="FinanzasPersonales.NET.Views.EstadisticaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Finanzas Personales - Estadísticas"
        Height="720"
        MinHeight="560"
        Width="680"
        MinWidth="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- ── Fila 0: Botones ── -->
        <StackPanel Grid.Row="0">

            <TextBlock Text="ESTADÍSTICAS FINANCIERAS"
                       FontSize="24"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,8" />

            <TextBlock Text="Resumen de tu situación financiera"
                       FontSize="14"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,18" />

            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="8" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="📊 Resumen General"    Height="34" Click="BtnResumenGeneral_Click" />
                <Button Grid.Column="2" Content="🏷️ Por Categoría"     Height="34" Click="BtnGastosPorCategoria_Click" />
                <Button Grid.Column="4" Content="📅 Resumen Mensual"    Height="34" Click="BtnResumenMensual_Click" />
                <Button Grid.Column="6" Content="📋 Ver Todo"           Height="34" Click="BtnVerTodo_Click" />
            </Grid>

            <Grid Margin="0,0,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="txtMensaje"
                           Grid.Column="0"
                           Text=""
                           TextWrapping="Wrap"
                           VerticalAlignment="Center" />
                <Button Grid.Column="2"
                        Content="← Volver"
                        Height="32"
                        Width="100"
                        Click="BtnVolver_Click" />
            </Grid>

        </StackPanel>

        <!-- ── Fila 1: Tabla de datos ── -->
        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="txtTablaTitulo"
                       Grid.Row="0"
                       Text=""
                       FontWeight="Bold"
                       FontSize="13"
                       Margin="0,0,0,6" />

            <DataGrid x:Name="dgDatos"
                      Grid.Row="1"
                      AutoGenerateColumns="True"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      GridLinesVisibility="All"
                      AlternatingRowBackground="#F0F4FF"
                      RowBackground="White"
                      BorderBrush="#CCCCCC"
                      BorderThickness="1"
                      ColumnHeaderHeight="32"
                      RowHeight="28"
                      SelectionMode="Single"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto" />
        </Grid>

    </Grid>
</Window>